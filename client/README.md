# קליינט הצ'אט (React)

ממשק צ'אט בעברית ו-RTL לשרת ניהול המשימות. ראו את ה-README הראשי בשורש הפרויקט להוראות הרצה מלאות.

```powershell
npm install
npm run dev
```

כתובת ה-API נקבעת דרך `VITE_API_URL` בקובץ `.env` (ברירת מחדל: `http://127.0.0.1:8000/chat`).
